const initialState = {
  astonomyPicture: [],
  peopleInSpace: [],
  issLocation: [],
  isError: false,
  isPicFetching: false,
  isPicFetched: false
}

const asyncReducer = (state = initialState, action) => {
  switch (action.type) {

  };
};

export default asyncReducer;